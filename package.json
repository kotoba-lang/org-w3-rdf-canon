{
  "private": true,
  "description": "npm dependency of the :cljs path only. multiformats.core reaches for @noble/hashes on :cljs. The classpath is derived from deps.edn via `clojure -Spath` rather than written out as sibling paths: sibling checkouts exist in the west workspace but not in CI, which is how the first version of this broke in two other repos here.",
  "dependencies": {
    "@noble/hashes": "2.0.1"
  },
  "scripts": {
    "smoke": "nbb --classpath \"$(clojure -Spath | tr ':' '\\n' | grep -E '(^src$|gitlibs)' | tr '\\n' ':')\" test/nbb_smoke.cljs"
  }
}
